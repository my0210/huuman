import Foundation

// Copy this file to Secrets.swift (which is gitignored) and fill in your values.
// These are Supabase anon keys (public, protected by RLS).
enum AppSecrets {
    static let supabaseURL = "https://YOUR_PROJECT.supabase.co"
    static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"
}
